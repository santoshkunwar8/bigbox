import {styled} from "styled-components"

export const AllRoomsWrapper = styled.div`

display:flex;
flex-direction:column;
gap:1rem;
.allRoomsWrapper{
    width:100%;
    display:flex;
    flex-wrap:wrap;
    gap:1rem;

}

`